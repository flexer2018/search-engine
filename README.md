# Toy web crawler and search engine

Requirements
-----------------

- Tested with Python 3.5.2 on Linux
- See requirements.txt file for required python packages. To install them run `pip install -r /path/to/requirements.txt`

Usage
-------------

